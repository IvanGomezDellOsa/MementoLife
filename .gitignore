# ========================
# SISTEMA Y IDEs (Global)
# ========================
.DS_Store
Thumbs.db
.history
.env
.env.*
!.env.example
.idea/
.vscode/
*.swp
*.code-workspace

# Logs
*.log

# ========================
# EXTENSIÓN DE CHROME (Node / TypeScript)
# ========================
node_modules/
extension/dist/
extension/build/
extension/*.zip

# Módulos TS generados desde content/ y render-core/ por scripts/gen-data.ts.
# Fuente de verdad: los JSON. No se duplican a mano ni se commitean.
extension/src/data/

# Salidas de Playwright (los goldens PNG de e2e/golden/ SÍ se commitean; esto es el ruido)
extension/test-results/
extension/playwright-report/
extension/blob-report/

# ========================
# PYTHON (sólo herramientas de build que se corren a mano:
# pyftsubset para el subset de Fraunces. El .woff2 resultante SÍ se commitea.)
# ========================
.venv/
__pycache__/
*.py[cod]

# ========================
# DOCUMENTACIÓN INTERNA (no publicada)
# ========================
# docs/ queda local por decision de Ivan. Los READMEs y PRIVACY SI se publican: la
# politica de privacidad es requisito de la tienda (tiene que ser una URL accesible) y
# sin README el repo publico no explica nada.
docs/

# ========================
# HERRAMIENTAS / SESIONES TEMPORALES
# ========================
.claude/
.cowork/
*_probe.txt

# Pagina de revision visual: se regenera con `npm run preview`
extension/preview/
