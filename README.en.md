[Español](README.md) | [English](README.en.md)

<p align="center">
  <img src="extension/brand/hourglass.png" alt="" width="120">
</p>

# MementoLife — A life calendar for Chrome's new tab

Chrome extension that replaces the new tab page with your entire life drawn as a grid of
weeks: the ones you have lived, filled in; the ones ahead, barely there. An empty ring marks
the week you are in right now. No motivational quotes, no reminders, nothing to do — just
the shape of your life, every time you open a tab.

---

## 📸 Screenshots

| | |
|---|---|
| ![Dark theme](extension/brand/store/1-dark.png) | ![Light theme](extension/brand/store/2-light.png) |
| ![Options page](extension/brand/store/3-opciones.png) | ![Fact of the day](extension/brand/store/4-efemeride.png) |

---

## 🛠️ Tech Stack

| Layer | Technology |
|------|------------|
| **Language** | Strict TypeScript, no `any` |
| **Runtime** | No framework, no bundler — native ESM, `tsc` as the only build step |
| **Render** | SVG generated by a pure core (no DOM, no `chrome.*`, never reads the clock) |
| **Testing** | Vitest (unit + snapshots) + Playwright (e2e against the real package) |
| **Packaging** | Manifest V3, a single permission (`storage`), no service worker |
| **CI** | GitHub Actions — typecheck, tests, e2e and font coverage on every push |

---

## 🎯 Features

**Visualization**
- Grid of weeks lived and left, computed from the date of birth
- Life expectancy adjustable between 20 and 100 years
- Empty ring marking the current week

**Personalization**
- Light, dark, or system theme
- Fully bilingual, Spanish and English, across the whole interface — not just the content
- Daily historical fact (366 dates, bilingual), can be turned off

**Privacy**
- Zero network connections — verified by an automated test that fails on any request
- A single permission: `storage`. No `host_permissions`, no service worker, no content scripts
- Date of birth is stored with `chrome.storage.local`, never `chrome.storage.sync`, so it never travels through your Google account

Full detail in [PRIVACY.en.md](PRIVACY.en.md).

---

## 🏗️ Architecture

```
extension/src/core/     pure logic: no DOM, no chrome.*, never reads the clock
extension/src/          the pages: newtab, options, preferences
render-core/            design-tokens.json — single source of design truth
content/efemerides/     the 366 historical facts, in Spanish and English
```

The core is pure on purpose: the date always comes in as a parameter. That is what makes the
tests deterministic and lets the visual regression gate be text (snapshots), not pixels.

The grid is 4160 dots, but the DOM holds **7 nodes**: three `<path>` elements —past, future
and ring— are accumulated instead of emitting one element per cell. Measured against the
naive alternative: 1.30 ms against 10.00 ms, 594 times fewer nodes.

---

## Install

```bash
cd extension && npm ci && npm run build
```

Then, in `chrome://extensions`: enable **Developer mode**, choose **Load unpacked**, and
pick `extension/build`.

> In incognito windows, Chrome does not allow any extension to override the new tab page.
> That is a platform restriction, documented and not worked around.

## Development

```bash
npm run typecheck   # strict tsc, no any
npm test            # unit tests + snapshots
npm run e2e         # Playwright against the built package
npm run build       # installable package in build/
```

---

## Project history

MementoLife started as an Android app that put the grid on the lock screen. It was finished
and working, but hit an unfixable platform limitation: MIUI and One UI silently ignore
attempts to update only the lock screen (`WallpaperManager.setBitmap(..., FLAG_LOCK)` does
not fail, it simply does nothing). Rather than break one of the project's design decisions,
the platform changed. The Android code is frozen at the `android-v1-final` tag.

---

## 📝 Development Notes

Development assisted by LLMs for the renderer implementation, tests, and build
infrastructure. The decisions that define the product — the platform pivot from Android to
Chrome extension, the final visual composition, the color palette, the cell-driven grid
geometry to avoid the optical vibration effect, the zero-network privacy model, and every
design iteration validated by eye against real screenshots — were made and directed by me.

---

## 👤 Author

**Iván Gómez Dell'Osa**

- Email: [ivangomezdellosa@gmail.com](mailto:ivangomezdellosa@gmail.com)
- LinkedIn: [linkedin.com/in/ivangomezdellosa](https://www.linkedin.com/in/ivangomezdellosa/)
- GitHub: [IvanGomezDellOsa](https://github.com/IvanGomezDellOsa)

## License

MIT.
